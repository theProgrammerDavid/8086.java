package emulator;

import emulator.Cpu;

public class Emulator {
  private Cpu cpu;

  public Emulator() { this.cpu = new Cpu(); }

  /**
   *
   * @param code String
   */
  public void loadCode(final String code) {}
}